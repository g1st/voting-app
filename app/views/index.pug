extends layout

block content
  if user
    h1 Hello, #{user.username}!
    
    h3 Here are your 
      a(href='/my_polls') polls
  else
    h1 Hey, stranger!
    h2
      | You can vote 
      a(href='/polls') here
      |  or 
      a(href='/oauth/register') register
      |  and create your own polls!
